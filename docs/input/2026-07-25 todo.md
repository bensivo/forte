Collecting some items from previous input files, and making a todo list for now:

- x Finalize my style guide
- x Refactor the entire codebase to be more structured and organized, following my style guide
- tweak: Add -h to all commands, instead of just --help
- tweak: Make sure links are serialized into entity pages in the MD
- feature: Implement a basic 'forte search', which does simple substring search on all docs and entities. Try to use just ripgrep or something for really fast searches wtihout indexes. 
- feature: Store forte configs in the user dir, not local. So users don't have to constantly navigate to their forte vault to put docs in it
- fix: Add entity schema to mentions, if not done already
- fix: Add output to agent ingest showing all the things created
- fix: Add JSON output to all commands
- idea: Add some features to make manual doc management better, so you can use it as 'just a doc folder' sometimes. 
- feature: Design a desktop GUI, so you can use it like an obsidian 
